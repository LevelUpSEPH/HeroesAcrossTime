using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class CowboyUltimateSkill : CharacterSkillBase // shoots a high noon bullet, if it hit; the bullet insta kills and resets cooldown
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
